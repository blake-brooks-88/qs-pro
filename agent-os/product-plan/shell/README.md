# Shell

Query++ uses a minimal, iframe-friendly shell:

- Thin header with branding + theme toggle
- No primary navigation (single workspace)
- Full-bleed content area for the IDE

Files in `shell/components/` are reference components for implementing the chrome around the workspace.

